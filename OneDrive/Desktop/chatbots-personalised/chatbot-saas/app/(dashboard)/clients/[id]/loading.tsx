import { RouteLoading } from "@/components/dashboard/route-loading";

export default function ClientDetailLoading() {
  return (
    <RouteLoading
      title="Loading client workspace"
      description="Preparing branding settings, embed details, and the live widget preview."
    />
  );
}
